# 宿舍管理系统

数据结构课程设计项目 —— 基于 C++ 实现的宿舍信息管理系统。

## 功能概览

| 模块 | 功能 | 实现 |
|------|------|------|
| 信息管理 | 增加、删除、修改、显示 | 动态数组 + 线性查找 |
| 信息查找 | 顺序查找、折半查找、分块查找 | 三种查找算法 |
| 信息排序 | 冒泡排序、快速排序、直接插入排序 | 三种排序算法 |
| 数据持久化 | 启动自动加载、手动/退出保存 | 文本文件读写 |

## 快速开始

```bash
# 编译（任选其一）
make              # Linux / macOS / WSL
mingw32-make      # Windows + MinGW

# 运行
./dormitory       # Linux / macOS
dormitory.exe     # Windows

# 清理构建产物
make clean        # 或 mingw32-make clean
```

## 项目结构

```
dormitory-system/
├── include/
│   ├── student.h          # 数据结构定义（Student, DormSystem）
│   ├── fileio.h           # 文件读写接口
│   ├── search.h           # 查找算法接口
│   ├── sort.h             # 排序算法接口
│   └── ui.h               # 菜单界面接口
├── src/
│   ├── fileio.cpp         # 数据文件加载与保存
│   ├── search.cpp         # 顺序查找 / 折半查找 / 分块查找
│   ├── sort.cpp           # 冒泡排序 / 快速排序 / 插入排序
│   └── ui.cpp             # 增删改查操作、菜单系统
├── main.cpp               # 程序入口
├── data.txt               # 学生数据文件（预置 12 条测试数据）
├── Makefile               # 构建配置
└── report/
    └── 课程设计报告.md     # 课程设计报告
```

## 数据格式

`data.txt` 使用 `|` 分隔字段，每行一条记录：

```
学号|姓名|性别|楼号|房间号|床位号|联系电话|专业
```

示例：
```
2024001|张三|男|A|101|1|13800001111|计算机科学
```

## 算法说明

### 查找

| 算法 | 用途 | 时间复杂度 |
|------|------|-----------|
| 顺序查找 | 按任意字段查找 | O(n) |
| 折半查找 | 按学号精确查找（需先排序） | O(log n) |
| 分块查找 | 按楼号分块查找 | O(√n) |

### 排序

| 算法 | 排序依据 | 时间复杂度 |
|------|---------|-----------|
| 冒泡排序 | 学号（升序） | O(n²) |
| 快速排序 | 姓名（升序） | O(n log n) |
| 直接插入排序 | 楼号 + 房间号 | O(n²) |

## 编译环境

| 平台 | 编译器 | 构建工具 |
|------|--------|----------|
| Windows | [MinGW-W64](https://www.mingw-w64.org/) g++ | `mingw32-make` |
| Linux | g++ | `make` |
| macOS | g++ / clang++ | `make` |

- 需支持 C++11（`-std=c++11`）
- Windows 下程序已内置 UTF-8 编码处理，中文显示不会乱码
